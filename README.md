# VocabsCapy releases

Android APK downloads for VocabsCapy, a Finnish vocabulary app with a capybara companion.
The app reads `latest.json` to offer in-app updates. Source code lives in a separate private repo.

Install: open the APK link on your Android phone, allow installs from this source once, tap Install.
